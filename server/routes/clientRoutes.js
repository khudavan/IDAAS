// server/routes/clientRoutes.js
import express from 'express';
const router = express.Router();

// Placeholder endpoints for OAuth clients management (Week 6+)
router.get('/', (_req, res) => res.json({ message: 'clients endpoint placeholder' }));

export default router;
